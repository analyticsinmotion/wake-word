.vscode/**
.vscode-test/**
.github/**
.claude/**
docs/**
src/**
node_modules/**
# The extension host's one runtime dependency. The app-root-path package it
# declares is never loaded by it, so it is not shipped.
!node_modules/sentencepiece-js/**
prototype/**
.gitignore
tsconfig.json
AGENTS.md
scripts/**
tests/**
coverage/**
vitest.config.mts
eslint.config.mjs
test-*.js
**/*.ts
**/*.map
# The native engine's source. The built engine and the runtime files it loads
# are staged into bin/, which ships, so no line here may exclude bin/.
engine-rs/**
